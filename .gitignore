# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg
.eggs/

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/

# OS
.DS_Store
Thumbs.db

# Claude Code (local only — not pushed to remote)
.claude/

# Data artifacts (large files)
*.npy
*.npz
*.pkl
*.pt
*.csv
*.jsonl
!examples/*.csv

# Jupyter
.ipynb_checkpoints/

# Environment
.env
.env.local

# Throwaway tool scratch (not pushed)
.playwright-mcp/

# Internal process/triage scratch — local only, not for (soon-public) history
docs/superpowers/

# Transient/internal narrative + rendered status docs (NOT the manuscript).
# Manuscript prose (manuscript/sections/*.md, evaluation_protocol.md, outline.md,
# proposals/, supplementary/, key_validated_results.md) and standard package docs
# (README, CHANGELOG, CONTRIBUTING, CODE_OF_CONDUCT) STAY tracked: git diffs of the
# prose complement TRACE (git = exact text/revert, TRACE = the why). Precise, not a
# blanket *.html/*.md, so future figure HTML/Markdown sources are not dropped.
HANDOFF_NEXT_SESSION.md
manuscript/manuscript_completion_checklist.html
